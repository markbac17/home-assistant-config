substitutions:
  name: "bt-proxy-a"
  friendly_name: "BT Proxy A"

packages:
  device_base: !include modules/device_base.yaml
  wifi: !include modules/wifi.yaml

esp32:
#  board: wemos_d1_mini32
  board: esp-wrover-kit
  framework:
    type: esp-idf

logger:
  level: DEBUG

esp32_ble_tracker:
  scan_parameters:
    active: true

bluetooth_proxy:
  active: true

button:
- platform: safe_mode
  name: Safe Mode Boot
  entity_category: diagnostic

ota:
  - platform: esphome
#  password: !secret ota_password